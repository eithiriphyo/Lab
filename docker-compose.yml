version: "3.9"

services:
  emberwok:
    build:
      context: .
      dockerfile: Dockerfile
    image: emberwok:local
    container_name: emberwok
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
    restart: unless-stopped
